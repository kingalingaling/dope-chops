import Card from '../components/Card';

const Account = () => {
    return (
        <section className="h-screen flex justify-center items-center bg-gradient-to-br from-orange-600 to-white">
            <Card />
        </section>
    )
}

export default Account;