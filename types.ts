export interface foodDetails {
    id: number,
    name: string,
    category: string,
    image: string,
    info: string,
    num: number,
    price: number,
    priceRange: string
    featured: boolean
}